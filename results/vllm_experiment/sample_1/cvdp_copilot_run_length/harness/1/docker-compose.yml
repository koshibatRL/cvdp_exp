networks:
  default:
    external: true
    name: licnetwork
services:
  1-new-tb:
    command: pytest -s -o cache_dir=/code/rundir/.cache /src/test_runner.py -v
    env_file: ./src/.env
    image: ghcr.io/hdl/sim/osvb
    volumes:
    - ./src/:/src/:ro
