networks:
  default:
    external: true
    name: licnetwork
services:
  auto:
    command: 'sh -c "pip install aes && pytest -s -o cache_dir=/rundir/harness/.cache
      /src/test_runner.py -v"

      '
    env_file: ./src/.env
    image: ghcr.io/hdl/sim/osvb
    volumes:
    - ./src:/src/:ro
