networks:
  default:
    external: true
    name: licnetwork
services:
  direct:
    build: .
    command: pytest -s --log-cli-level=INFO -o cache_dir=/rundir/harness/.cache /src/test_runner.py
      -v
    env_file: ./src/.env
    volumes:
    - ./src/:/src/:ro
